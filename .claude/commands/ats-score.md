---
description: Score a resume PDF against HackerRank's open-source ATS rubric
argument-hint: [pdf-path | --all]
---

Run the **ats-score** skill (`.claude/skills/ats-score/SKILL.md`) with
`$ARGUMENTS` as the target — a PDF path (defaults to `build/resume.pdf` if
omitted), or `--all` to batch-score the archived corpus under
`~/Desktop/Tailored Resumes/`. Follow that skill's steps exactly.
