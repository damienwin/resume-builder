---
description: Generate a tailored one-page resume for a given job description
argument-hint: <job-url-or-jd-file-path>
---

Run the **tailor-resume** skill (`.claude/skills/tailor-resume/SKILL.md`)
with `$ARGUMENTS` as the job description input (URL, file path, or pasted
text). Follow that skill's steps exactly, including the per-user overrides
in `knowledge/rules.md` if present.
