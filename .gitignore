build/
*.pdf
*.aux
*.log
*.out
*.synctex.gz
jd.txt
.DS_Store

# marker: /start has already shown its one-time star nudge in this clone
.star-prompted

# personal content — never publish real resume data
knowledge/

# third-party ATS scoring tool + its output — cloned/generated, not vendored
tools/
eval/
