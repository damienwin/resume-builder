build/
*.pdf
*.aux
*.log
*.out
*.synctex.gz
jd.txt
.DS_Store

# personal content — never publish real resume data
knowledge/

# third-party ATS scoring tool + its output — cloned/generated, not vendored
tools/
eval/
